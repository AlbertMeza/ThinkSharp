public static Card[] MakeDeck()
{
  Card[] newDeck = new Card[52];
  int index = 0;
  for (int suit = 0; suit <= 3; suit++)
    {
      for (int rank = 1; rank <= 13; rank++)
        {
          newDeck[index] = new Card(suit, rank);
          index++;
        }
    }
    return newDeck;
}        
