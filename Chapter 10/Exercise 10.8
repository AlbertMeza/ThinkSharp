1. Output: 30

2. 
To Mus

Main: int[] bob = Make(5); Console:
Make(5): int[] a = new int[n]; Console:
Make(5): for(int i = 0; i < n; i++) Console:
Make(5): a[i] = i + 1; Console: //creates the array values of 1,2,3,4,5
Make(5): a;
Main: Dub([1,2,3,4,5]); Console: 
Dub(array): for(int i = 0; i < jub.Length; i++); Console:
Dub(array): array[i] *=2; Console: //doubles the values of the array
Main: Console.WriteLine(Mus(bob)); Console:
Mus([2,4,6,8,10]): int fus = 0; Console: 
Mus(bob): foreach(int z in zoo) Console: //this is a loop that adds the array's total
Mus(bob): fus += z; Console: 
Mus(bob: return fus; Console:
Main: done Console: 30

3.
Mus is a method that adds the total of the values within an integer array
