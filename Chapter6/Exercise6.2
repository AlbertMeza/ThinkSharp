using System;

namespace Chapter6
{
    internal class Program
    {

        static void Main(string[] args)
        {
            double count = 0.0;
            while (count < 1)
            {
                count = count + 0.1;
                count = Math.Round(count,1);
                Console.Write(count);
                Console.ReadLine();
            }
        }

    }
}

1. count is base 2 so it never reaches a perfect 1.0, it's similar to how in base 10, there is no such way to display 1/3 properly (0.33333333)
2. see code above
